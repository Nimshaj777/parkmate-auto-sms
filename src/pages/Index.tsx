// Update this page (the content is just a fallback if you fail to update the page)

import { ParkingSMSApp } from "@/components/ParkingSMSApp";

const Index = () => {
  return <ParkingSMSApp />;
};

export default Index;
